selectors:
  - name: dim_except_listings_w_hosts
    description: Select all dim models, excluding dim_except_listings_w_hosts.
    definition:
      union:
        - method: path
          value: models/dim
        - exclude:
            - method: fqn #fully qualified name
              value: dim_except_listings_w_hosts